# Backend Environment Variables

# MongoDB Connection URI
# Local: mongodb://127.0.0.1:27017/cloudbase_voting
# Atlas: mongodb+srv://<username>:<password>@cluster.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGO_URI=mongodb://127.0.0.1:27017/cloudbase_voting

# Server Port
PORT=5000

# JWT Secret Key (CHANGE THIS IN PRODUCTION!)
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# JWT Token Expiration (default: 7d)
JWT_EXPIRY=7d

# Node Environment
NODE_ENV=development

# CORS Origins (comma-separated)
CORS_ORIGIN=http://localhost:3000

# Admin Registration Key (Required to register as admin)
# Generate a unique key for your organization
ADMIN_SECRET_KEY=your_admin_secret_key_here
